# Django secret key. Replace with a long random string in production.
DJANGO_SECRET_KEY=dev-jakeala-naturals-change-in-production

# 1 = development (DEBUG on), 0 = production
DJANGO_DEBUG=1

# Comma-separated allowed hosts (used when DEBUG is off)
ALLOWED_HOSTS=localhost,127.0.0.1
